# Build artifacts.
/target/

# Cargo.lock IS committed for this workspace because we ship binaries
# and need reproducible builds. Do NOT add Cargo.lock here.

# Editor / OS noise.
*.tmp
.DS_Store
review.html
